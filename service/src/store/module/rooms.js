import * as types from '../mutation-types'

const state = {

}

const mutations = {
  [types.DEAL] (state, roomId) {
    console.log('OK')
    [types.DEAL] (state, roomId)
  }
}

export default {
  state,
  mutations
}
